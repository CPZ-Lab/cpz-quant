[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cpz-risk-rs"
version = "0.1.0"
description = "Rust acceleration for CPZ Risk Analytics"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
